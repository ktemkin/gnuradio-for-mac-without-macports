diff --git a/configure.in b/configure.ac
similarity index 100%
rename from configure.in
rename to configure.ac
diff --git a/Makefile.in b/Makefile.in
index a7cbddf..dfaccf0 100644
--- a/Makefile.in
+++ b/Makefile.in
@@ -44,7 +44,7 @@ SDLTEST_OBJECTS = @SDLTEST_OBJECTS@
 
 WAYLAND_SCANNER = @WAYLAND_SCANNER@
 
-SRC_DIST = *.txt acinclude Android.mk autogen.sh android-project build-scripts cmake cmake_uninstall.cmake.in configure configure.in debian docs include Makefile.* sdl2-config.cmake.in sdl2-config.in sdl2.m4 sdl2.pc.in SDL2.spec.in src test VisualC.html VisualC VisualC-WinRT Xcode Xcode-iOS
+SRC_DIST = *.txt acinclude Android.mk autogen.sh android-project build-scripts cmake cmake_uninstall.cmake.in configure configure.ac debian docs include Makefile.* sdl2-config.cmake.in sdl2-config.in sdl2.m4 sdl2.pc.in SDL2.spec.in src test VisualC.html VisualC VisualC-WinRT Xcode Xcode-iOS
 GEN_DIST = SDL2.spec
 
 ifneq ($V,1)
@@ -125,11 +125,6 @@ LT_LDFLAGS  = -no-undefined -rpath $(DESTDIR)$(libdir) -release $(LT_RELEASE) -v
 
 all: $(srcdir)/configure Makefile $(objects) $(objects)/$(TARGET) $(objects)/$(SDLMAIN_TARGET) $(objects)/$(SDLTEST_TARGET)
 
-$(srcdir)/configure: $(srcdir)/configure.in
-	@echo "Warning, configure.in is out of date"
-	#(cd $(srcdir) && sh autogen.sh && sh configure)
-	@sleep 3
-
 Makefile: $(srcdir)/Makefile.in
 	$(SHELL) config.status $@
 
commit 09ea0242fc579ed68c916a15ccb409ed96c7d1ca
Author: Christopher Friedt <chrisfriedt@gmail.com>
Date:   Sun Jun 4 18:44:19 2017 -0700

    autogenerated aclocal.m4

diff --git a/configure.ac b/configure.ac
index f186915..8feb79f 100644
--- a/configure.ac
+++ b/configure.ac
@@ -4,6 +4,14 @@ AC_CONFIG_HEADER(include/SDL_config.h)
 AC_CONFIG_AUX_DIR(build-scripts)
 AC_CONFIG_MACRO_DIR([acinclude])
 
+AC_DEFINE([HAVE_SA_SIGACTION], [], [Description])
+AC_DEFINE([HAVE_SEM_TIMEDWAIT], [], [Description])
+AC_DEFINE([SDL_AUDIO_DRIVER_NACL], [], [Description])
+AC_DEFINE([SDL_VIDEO_DRIVER_NACL], [], [Description])
+AC_DEFINE([SDL_VIDEO_DRIVER_X11_CONST_PARAM_XEXTADDDISPLAY], [], [Description])
+AC_DEFINE([SDL_VIDEO_DRIVER_X11_SUPPORTS_GENERIC_EVENTS], [], [Description])
+AC_DEFINE([SDL_VIDEO_DRIVER_X11_XINPUT2_SUPPORTS_MULTITOUCH], [], [Description])
+
 dnl Save the CFLAGS to see whether they were passed in or generated
 orig_CFLAGS="$CFLAGS"
 
diff --git a/configure.ac b/configure.ac
index db6afd6..8dd0294 100644
--- a/configure.ac
+++ b/configure.ac
@@ -41,6 +41,7 @@ AC_SUBST(SDL_BINARY_AGE)
 AC_SUBST(SDL_VERSION)
 
 # libtool versioning
+LT_PREREQ(2.4)
 LT_INIT([win32-dll])
 
 LT_RELEASE=$SDL_MAJOR_VERSION.$SDL_MINOR_VERSION
diff --git a/configure.ac b/configure.ac
index 8dd0294..8abe3e6 100644
--- a/configure.ac
+++ b/configure.ac
@@ -1,8 +1,8 @@
 dnl Process this file with autoconf to produce a configure script.
 AC_INIT(README.txt)
 AC_CONFIG_HEADER(include/SDL_config.h)
-AC_CONFIG_AUX_DIR(build-scripts)
-AC_CONFIG_MACRO_DIR([acinclude])
+dnl AC_CONFIG_AUX_DIR(build-scripts)
+dnl AC_CONFIG_MACRO_DIR([acinclude])
 
 AC_DEFINE([HAVE_SA_SIGACTION], [], [Description])
 AC_DEFINE([HAVE_SEM_TIMEDWAIT], [], [Description])
